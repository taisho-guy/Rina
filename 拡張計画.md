## 拡張計画

### 前提差異の総括(本ターンで新規確認した3項目を追加)

| 概念 | NiVE3 | NeoUtl現状 | 該当ファイル |
|---|---|---|---|
| フッテージ共有 | `FootageId`で複数レイヤーが1メディアを参照、実体は別管理 | `MediaSource{path: PathBuf}`をエンティティへ直接埋め込み、共有プール不在 | `src/ecs/components/shape_misc.rs` |
| カメラ/ライト | `IsCamera`/`IsLight`フラグ付きレイヤーを任意個数配置、独立3Dオブジェクト | `GroupControl`のcurtainチェーン内`ControllerKind::Group{camera: Some(cam)}`経由の単一カメラのみ、独立オブジェクトタイプなし | `src/ecs/systems/camera.rs` |
| パラメータプリセット | `EffectPreset.cs`による名前付き保存・再適用 | 該当機構なし(検索結果0件) | — |

---

### 全フェーズ一覧(依存順)

**A系統: シェーダーABI(`neoutl-effect-api`/`neoutl-object-api`)**

| Phase | 内容 | 対象 | ゼロコピー |
|---|---|---|---|
| 1 | ROI計算 + キャッシュ判定 | `EffectVTable`に`calc_roi`/`is_need_render_frame`追加 | 維持(スカラー値渡し) |
| 2 | 音声処理経路 | `EffectKind`追加、`process_audio`追加 | 維持(`*mut f32`直接書換) |
| 3 | プロパティ編集通知/書き戻し | `PropertyWriteback`構造体、`on_property_edited`/`on_property_restored`/`poll_writeback`追加 | 維持(スカラー配列) |
| 4 | メタデータ拡張 | `EffectMeta`へ`author`/`description`/`uuid`/`is_dummy`/`use_composition_camera`追加 | 維持(`StrRef`参照) |
| 6 | Expression連携 | 新規crate`neoutl-expression-api`、`bind_expression_host`追加 | 維持(スカラー往復) |
| 7 | デバイス動的再設定 | 両VTableへ`setup_accelerator`追加 | 維持(ハンドル1個) |

**B系統: ECSコンポーネント拡張(`src/ecs/components/`)**

| Phase | 内容 | 新規ファイル | ゼロコピー |
|---|---|---|---|
| 5 | 親子関係 + トラックマット + 調整レイヤー | `hierarchy.rs`: `ParentEntity`, `TrackMatteSource`, `AdjustmentLayer` | 維持(GPUテクスチャハンドル参照) |
| 8 | マスク + ブレンドモード + レイヤートグル | `mask.rs`: `MaskStack`, `BlendMode`、`LayerStates`を6要素へ拡張 | 維持(enum/bool/ID参照) |
| 9 | タイムリマップ + プロパティ分離 | `time_remap.rs`: `TimeRemap`、`ObjectMeta.property_schema`を`property_groups`へ変更 | 維持(配列参照) |

**C系統: 本ターン新規追加**

| Phase | 内容 | 対象 | ゼロコピー |
|---|---|---|---|
| 10 | フッテージ共有プール | 新規`src/ecs/resources/footage_pool.rs`: `FootagePool { entries: HashMap<FootageId, FootageEntry> }`、`FootageEntry { path: PathBuf, ref_count: u32, decoded_handle: *const () }`。`MediaSource`を`path: PathBuf`から`footage_id: FootageId`へ変更し実体参照は`FootagePool`経由に一元化。 | 維持(デコード済みハンドルをエンティティ間で共有、複製なし) |
| 11 | カメラ/ライト独立オブジェクト化 | 新規`crates/objects/camera/`, `crates/objects/light/`(既存`crates/objects/scene`, `crates/objects/shape`と同型構成)。`ObjectMeta`に`CAMERA_STABLE_ID`, `LIGHT_STABLE_ID`追加。`src/ecs/systems/camera.rs`の`resolve_camera`を、curtainチェーン限定から`KindId`が`CAMERA_STABLE_ID`の全エンティティ走査へ置換、複数カメラのシーン内切替対応。 | 維持(行列演算のみ) |
| 12 | プリセット保存機構 | 新規`src/ecs/resources/preset_store.rs`: `PresetStore { entries: HashMap<PresetId, PresetData> }`、`PresetData { effect_uuid: String, params: Vec<(String, f32)> }`をJSONシリアライズ、`~/.config/neoutl/presets/`へ保存。 | 該当外(スカラー永続化のみ) |
| 13 | Undo/History command層 | 新規`src/ecs/history.rs`: `HistoryCommand`トレイト(`apply`/`revert`)、`HistoryStack { done: Vec<Box<dyn HistoryCommand>>, undone: Vec<Box<dyn HistoryCommand>> }`。Phase3の`poll_writeback`の`is_user_action`フラグを本スタックへの記録トリガーとして接続。 | 該当外 |

---

### 実行順序

```
1 → 2 → 3 → 4           (エフェクトABI基礎)
5 → 8 → 9               (レイヤー構造、5は3完了後に着手: writeback連携のため)
10                      (9のMediaSource変更対象のため9完了後)
11                      (5のAdjustmentLayer/TrackMatte合成順序確定後)
6 → 7                   (Expression/デバイス、他フェーズと独立、並行着手可)
12                      (4のEffectMeta.uuid確定後、プリセットキーとしてuuid使用)
13                      (3の poll_writeback 実装後、最終フェーズ)
```

### 検証基準(全フェーズ共通)

1. `cargo build --workspace`で既存24エフェクトクレート・3オブジェクトクレート(shape/scene/camera/light追加後は5クレート)のリンク成功。
2. 画像・音声バッファ経路への`memcpy`相当処理混入なしを`grep -rn "\*const \|\*mut "`全洗い出しで確認。
3. Phase 10完了後、同一`FootageId`を2エンティティが参照する構成で`decoded_handle`のアドレス一致を確認(デコード処理が1回のみ実行された証跡)。
4. Phase 13完了後、`poll_writeback`経由の値変更が`HistoryStack.done`へ1件追加されることを確認、Undo実行で`is_user_action=false`の書き戻しが記録対象外であることを確認。