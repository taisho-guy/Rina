# NeoUtl 拡張計画（改訂版）

## 1. 基本方針・設計原則
NiVE3 / AviUtl / AfterEffects のワークフローと拡張性を取り入れつつ、NeoUtl 固有のコアアーキテクチャ（**ECS + カーテンチェーン + ゼロコピー GPU/CPU パイプライン**）との整合性を厳格に維持する。

- **ゼロコピー厳守**: バッファ経路への不要な `memcpy` や生ポインタ操作を混入させず、GPUテクスチャハンドル参照および行列・スカラー演算で完結させる。
- **二重実装の排除**: NeoUtl に既に存在する優れた機構（`MediaCache`、`ClipTarget`、`GroupControl` / `Framebuffer` カーテン等）がある場合、外部の概念（`TrackMatteSource`, `FootagePool` 等）を盲目的に追加せず、既存アーキテクチャへ統合・活用する。

---

## 2. 前提差異の総括（現状ステータス）

| 概念 | NiVE3 / AE | NeoUtl 実装状況 | 該当コンポーネント / ファイル | ステータス |
|---|---|---|---|---|
| **フッテージ共有** | `FootageId` で複数レイヤーが参照、実体は別管理 | `neoutl_media_runtime::cache::global()` によりパス単位でデコード結果をグローバル共有 | `crates/neoutl-media-runtime/src/cache.rs` | **既存機構で充足 (Phase 10 不要確認済)** |
| **カメラ / ライト** | 独立レイヤー・3Dオブジェクト | 独立オブジェクト (`CAMERA_STABLE_ID`, `LIGHT_STABLE_ID`) 化完了。複数カメラのレイヤー優先切替およびカーテンフォールバックに対応 | `crates/objects/camera/`<br>`crates/objects/light/`<br>`src/ecs/systems/camera.rs` | **完了 (Phase 11)** |
| **親子階層** | レイヤー間リンク | `ParentRef(pub usize)` によるトランスフォーム行列の伝播 | `src/ecs/components/hierarchy.rs`<br>`src/ecs/world/hierarchy_ops.rs` | **完了 (Phase 5)** |
| **トラックマット** | Alpha / Luma マット | `ClipTarget` (`ClipMode`: Alpha, AlphaInvert, Luminance, LuminanceInvert, Chroma) により同等以上の機能を実現 | `src/ecs/components/clip_target.rs`<br>`src/ecs/systems/active_query.rs` | **完了 (Phase 5/8 統合)** |
| **調整レイヤー** | 下位レイヤーへのエフェクト適用 | `ControllerKind::Framebuffer`（フレームバッファキャプチャ）とカーテンチェーンによる一括エフェクト適用 | `src/ecs/components/group_control.rs`<br>`src/ecs/systems/curtain.rs` | **完了 (Phase 5 統合)** |
| **レイヤー状態 / 合成** | 6フラグ、描画モード | `LayerState` 6要素 (`visible`, `locked`, `solo`, `shy`, `video_enabled`, `audio_enabled`)、`BlendMode` (Normal, Add, Multiply 等) 実装済 | `src/ecs/components/resources.rs`<br>`src/ecs/systems/types.rs` | **完了 (Phase 8)** |
| **タイムリマップ** | 時間伸縮・フリーズ | `TimeRemap` コンポーネント、イージング連携、フリーズフレーム評価 | `src/ecs/components/time_remap.rs` | **完了 (Phase 9)** |
| **プリセット管理** | パラメータ保存・再適用 | 未実装（JSONシリアライズによる永続化ストレージが必要） | `src/ecs/resources/preset_store.rs` (予定) | **未着手 (Phase 12)** |
| **Undo / History** | 操作の取り消し・再実行 | `PropertyWriteback`（変更通知）は実装済。コマンドスタック未実装 | `src/ecs/history.rs` (予定) | **未着手 (Phase 13)** |
| **Expression** | スクリプトによる動的駆動 | Luaエフェクトランタイムは存在するが、汎用ExpressionホストAPIは未定義 | `crates/neoutl-expression-api/` (予定) | **未着手 (Phase 6)** |
| **デバイス再設定** | GPUデバイス切替 | 静的設定のみ、動的再構築未対応 | `setup_accelerator` (予定) | **未着手 (Phase 7)** |

---

## 3. 全フェーズ進捗一覧

### A系統: シェーダー・エフェクト ABI (`neoutl-effect-api`, `neoutl-object-api`)

| Phase | 内容 | 対象 | ゼロコピー | ステータス |
|:---:|---|---|:---:|:---:|
| **1** | ROI計算 + キャッシュ判定 | `EffectVTable` に `calc_roi` / `is_need_render_frame` 追加 | 維持 (スカラー値渡し) | **完了** |
| **2** | 音声処理経路 | `EffectKind::Audio` 追加、`process_audio` 追加 | 維持 (`*mut f32` 直接書換) | **完了** |
| **3** | プロパティ編集通知/書き戻し | `PropertyWriteback` 構造体、`on_property_edited` / `on_property_restored` / `poll_writeback` 追加 | 維持 (スカラー配列) | **完了** |
| **4** | メタデータ拡張 | `EffectMeta` へ `author` / `description` / `uuid` / `is_dummy` / `use_composition_camera` 追加 | 維持 (`StrRef` 参照) | **完了** |
| **6** | Expression連携 | 新規 crate `neoutl-expression-api`、`bind_expression_host` 追加 | 維持 (スカラー往復) | **未着手** |
| **7** | デバイス動的再設定 | 両 VTable へ `setup_accelerator` 追加 | 維持 (ハンドル1個) | **未着手** |

### B系統: ECSコンポーネント・レイヤー構造 (`src/ecs/components/`, `src/ecs/systems/`)

| Phase | 内容 | 対象 | ゼロコピー | ステータス |
|:---:|---|---|:---:|:---:|
| **5** | 親子関係 + トラックマット + 調整レイヤー | `ParentRef`、`ClipTarget` (マット統合)、`GroupControl::Framebuffer` (調整レイヤー統合) | 維持 (GPUテクスチャ/行列参照) | **完了** |
| **8** | マスク + ブレンドモード + レイヤートグル | `LayerState` 6フラグ、`BlendMode`、`ClipTarget` によるマスク包括 | 維持 (enum/bool/ID参照) | **完了** |
| **9** | タイムリマップ + プロパティ分離 | `TimeRemap` コンポーネント実装済。プロパティグループ分離はUI要求に応じて実施 | 維持 (配列参照) | **タイムリマップ完了**<br>(グルーピング検討) |

### C系統: メディア・オブジェクト・システム層

| Phase | 内容 | 対象 | ゼロコピー | ステータス |
|:---:|---|---|:---:|:---:|
| **10** | フッテージ共有プール | `neoutl_media_runtime::cache::global()` でパス単位共有が既に実現されており、新規作成は二重実装となるためスキップ | 維持 | **不要（検証完了）** |
| **11** | カメラ/ライト独立オブジェクト化 | 新規 `crates/objects/camera/`, `crates/objects/light/`、`CAMERA_STABLE_ID`, `LIGHT_STABLE_ID`、アクティブカメラ解決、レイヤー優先度・切替対応 | 維持 (行列演算のみ) | **完了** |
| **12** | プリセット保存機構 | 新規 `src/ecs/resources/preset_store.rs`: `PresetStore`、`~/.config/neoutl/presets/` へJSON永続化 | 該当外 (スカラー永続化) | **完了** |
| **13** | Undo/History command層 | 新規 `src/ecs/history.rs`: `HistoryCommand` トレイト、`HistoryStack`。Phase 3 の `poll_writeback` 連携 | 該当外 | **完了** |

---

## 4. 残存フェーズの詳細仕様と実行計画

### 残存フェーズ 1: Phase 6 (Expression連携)
- **目的**: レイヤープロパティ間を数式やスクリプトでバインドし、連動アニメーションを実現する。
- **構成**:
  - 新規クレート `crates/neoutl-expression-api/`
  - `bind_expression_host(host_vtable)` を通じた値の評価と双方向同期。

### 残存フェーズ 2: Phase 7 (デバイス動的再設定)
- **目的**: 実行時における GPU アダプタ切替やロスト時の復帰を可能にする。
- **構成**:
  - `EffectVTable` / `ObjectVTable` に `setup_accelerator` を追加し、リソースの再バインドを行う。

---

## 5. 今後の実行順序

```
[完了] 1 → 2 → 3 → 4   (エフェクトABI基礎: 完了)
[完了] 5, 8, 9          (レイヤー構造・タイムリマップ: 完了)
[完了] 10 (不要検証済), 11 (カメラ/ライト独立オブジェクト: 完了)
[完了] 12               (プリセット保存機構: 完了)
[完了] 13               (Undo/History: Phase 3 poll_writeback 連携: 完了)
  ↓
Phase 6                (Expression連携クレート)
  ↓
Phase 7                (デバイス動的再設定)
```

---

## 6. 検証基準（全フェーズ共通）

1. **ワークスペースビルド**: `cargo build --workspace` で全クレートのリンク・コンパイルが警告・エラーなく通過すること。
2. **ゼロコピー維持**: 画像・音声バッファ経路への不要な `memcpy` や生ポインタ操作が混入していないことを `grep -rn "\*const \|\*mut "` 等で検証すること。
3. **互換性**: 既存のプロジェクトファイル・シリアライズフォーマットおよび従来のグループ制御／カーテンチェーンとの完全な後方互換性を維持すること。
4. **テストカバレッジ**: 新規機能ごとにユニットテスト・結合テストを作成し、`cargo test --workspace` が 100% 通過すること。